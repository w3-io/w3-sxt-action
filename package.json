{
  "name": "w3-sxt-action",
  "version": "1.0.0",
  "private": true,
  "description": "Space and Time decentralized SQL action for W3 workflows",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:write": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint src/",
    "test": "node --test test/*.test.js",
    "build": "ncc build src/index.js -o dist",
    "all": "npm run format && npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@w3-io/action-core": "^0.4.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@vercel/ncc": "^0.38.4",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0"
  }
}
